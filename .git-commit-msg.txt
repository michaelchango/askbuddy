修复变更更新场景：需求文档黑底未渲染 + 更新后按钮未恢复

- markdown-renderer：新增 unwrapMarkdownFence，渲染前去除 AI 包裹的外围 markdown 代码围栏，避免整篇文档被识别为代码块显示黑底
- requirement-shell：变更更新完成后延迟恢复输入框上方按钮模块；优先恢复流程推进后的 proceed_prompt，否则回退变更前快照，避免按钮错误变化或消失；兜底清空逻辑在变更刚完成时跳过一次；将 pendingPrompt 加入 processChangeQueue 依赖以正确捕获快照
- conversation-panel：更新状态文案移至输入框上方按钮模块，更新期间隐藏按钮
- step-navigator：移除顶部变更状态文案区块

# 待办事项（AskBuddy）

记录目前尚未处理、未来某个节点需要处理的事项（非开发进度日志）。

> 本文件于 2026-08-11 重写。原版本停留在 M0/M1 早期（仅记了 CloudBase 接入、对话创建需求），
> 早已严重滞后：M1–M3 实际已全部完成但从未登记，且"库(library)"等已交付功能完全未提及。
> 真实进度以本文 + `CHANGELOG.md` + `docs/phase1-*.md` 为准。下面先给出**已核实的真实状态**，
> 再列待办。

---

## 〇、已核实的真实完成状态（截至 2026-08-11，代码已落地）

- **M0 基础栈**：CloudBase Node SDK 接入（`lib/cloudbase`）、数据层 `lib/db` 双后端（`cloudbase.ts` 走 CloudBase **PostgreSQL 网关 B 通道 `exec-pgsql`**，`sql.ts` 为直连占位、`backend.ts` 当前路由到 `cloudbase`），`field-map.ts` 键映射、`lib/storage` 回落 NoSQL、Next.js App Router 框架、ESLint/`next build` 门禁通过。
- **M1 数据库 + 工作区**：
  - 推送原子化（`lib/db/cloudbase.ts`：Connection:close + 并发栅栏 3 + 重试 7 + `isTransientSqlError`；对话/阶段写入软降级）；`db-pushdown-parity.test.ts` 已写。
  - 需求工作区 `app/dashboard/requirements/[requirementId]/page.tsx` + `components/requirements/`（对话、卡片预览、DevContext、原型、proposal、引用、步骤导航、outputs 侧栏、requirement-shell 等）。
  - 需求创建页 `app/dashboard/requirements/page.tsx`（首条对话才创建；project/no-project 模式；`ConversationPanel` 待定态）。
  - API：`requirements/[id]/` 下 card / conversation(SSE) / design / dev-context / prd / prototype / research-analysis / steps / outputs。
- **M2 四步流水线 + 项目级**：
  - AI harness：`lib/ai/`（`client.ts` 路由/流式/降级/mock；`models.ts`；`prompts/` 12 个；`orchestrator.ts`；`steps/` 3 个 step；`connectors/internal/save-card.ts`；`context/builder.ts` + 测试）。
  - 流水线步骤与产物：调研分析 `lib/ai/steps/research-analysis.ts`、方案设计 `lib/ai/steps/design.ts`、PRD `lib/ai/steps/prd.ts`、原型 `lib/ai/steps/prototype.ts`（真实 HTML 生成 + 版本管理 `lib/services/prototypes.ts` + 分享链接）。
  - 产物聚合 `lib/services/outputs.ts`（card/research_analysis/design[含 solution+prototype]/prd + 版本）。
  - 项目级页面 `app/dashboard/projects/[projectId]/`（research / solution / prototype / prd / docs）。
  - 库（Library）`lib/services/library.ts` + `components/libraries/` + `app/api/library/*`（**原待办漏记，实际已完成**）。
- **M3 DevContext + 协作**：
  - DevContext 服务 `lib/services/devcontext.ts`（版本化落库 + 16 段内容 + 溯源/M3 分级注入）+ render/validate/source 三件套 + 3 个测试文件。
  - ~~HITL proposals 建议卡~~ `lib/services/proposals.ts` + `components/requirements/proposal-card.tsx`（**2026-08 已回退**：移除建议卡全链路，恢复「下一阶段确认闸门」，产物直写库）。
  - 分级溯源 `_source`（M0/M1/M2 三级）贯穿 cards/research/design/prd/devcontext。
  - MCP server `mcp/server.ts`（v2，实测注册 **13 个 tool**：list_projects / list_requirements / requirement_get / requirement_research / requirement_solution / requirement_prototype / requirement_prd / requirement_dev_context / 4 个 dev_context_* 分段工具 / search_knowledge）。
- **M4 知识复利（项目级知识库 + 语义检索）**：
  - Embedding 接入层 `lib/ai/embedding.ts`（腾讯 **TokenHub** OpenAI 兼容协议，模型 `kinfra-text-embedding-0.6b`，Bearer 鉴权，`EMBEDDING_DIMENSIONS=1024` 已由 `embedding-probe` 对真实 API 实测确认）。非对称密钥分离：生成走 `CLOUDBASE_SECRET`，embedding 走 `TOKENHUB_API_KEY`。TTL 60s 冷却 + 确定性 mock 降级。历史：混元直连（TC3 自签）因服务未开通已废弃并删除。
  - `knowledge_entries` 表（`db/schema.sql` + `db/drizzle/schema.ts`，`embedding vector(1024)` + category/source_type/status 三类 CHECK + 索引；`field-map.ts` 新增 `VECTOR_COLS`，`postgres.ts`/`cloudbase.ts` 支持向量列读写）。
  - 知识服务 `lib/services/knowledge.ts`（CRUD/软删/语义检索 `searchVector` 余弦下推/关键词兜底/召回/`backfillEmbeddings`）+ `lib/schemas/knowledge.ts`。
  - 知识沉淀 `lib/services/knowledge-extractor.ts`（从 M3 decisions 抽取，三道去重闸）+ `lib/ai/prompts/knowledge-extract.ts`（`knowledge_extract` 任务类型）+ 触发端点 `app/api/projects/[id]/knowledge/extract`。
  - RAG 注入：`lib/ai/context/builder.ts` 并行召回 + `lib/ai/context/budget.ts`（12000 字符预算裁剪）+ 5 个 prompt 渲染知识段 + DevContext `_source.knowledge_ids` 真实填值（幻觉 id 白名单过滤）。
  - 项目知识 API（`/api/projects/[id]/knowledge` 及 `/[entryId]`、`/search`、`/backfill`）+ MCP `search_knowledge`（callPlatform 支持 POST）+ 知识库管理 UI（`/dashboard/projects/[id]/knowledge` + `components/knowledge/` + project-shell 导航）。

---

## 一、接入真实 CloudBase（Phase 2）
- [x] 开 CloudBase 环境，配置 ApiKey（`CLOUDBASE_SECRET`，node-sdk `accessKey` 方式）。
- [x] `npm i @cloudbase/node-sdk` + `ws`。
- [x] 数据层接入 CloudBase **文档型（NoSQL）数据库**：`lib/db` 已接，`lib/cloudbase`、`lib/storage` 已接，`.env.local` 置 `USE_MOCK=false`。已验证「重启不丢数据」。
- [ ] Webify 部署（`next build` 已通过，待实际部署）。
- [ ] **（待定）评估是否改用 CloudBase SQL 型数据库**：见「四」。

## 二、延后任务（已确认暂时不改，后续按需处理）
- [ ] **对象存储 COS**：`COS_BUCKET` / `COS_REGION` 仍空。当前原型 HTML / 导出文件临时存 NoSQL，未走 COS。需接时再实现 `lib/storage` 的 COS 分支。
- [ ] **真实多用户 Auth**：当前固定开发用户 `mock-user-001`（`lib/auth/session.ts`）。需补一套客户端登录 UI（@cloudbase/js-sdk 的 signIn，邮箱/微信登录），是独立功能，未做。
- [x] **AI 模型服务（主链路已完成）**：真实 CloudBase AI 已端到端打通（对话→调研→方案→原型→PRD→DevContext 全链路，见「〇」）。保留 `AI_MOCK` 开关便于本地无凭证开发。
- [ ] **SQL 网关连接池（EMAXCONNSESSION）根治迁移：延后，按生产用户量触发**（用户决策 2026-08-07）：当前数据层经 CloudBase 网关 `exec-pgsql`（SQL 通道 session 模式）访问，环境级 `pool_size=10` 为整个 `CLOUDBASE_ENV_ID` 共享硬上限，并发高时偶发 `EMAXCONNSESSION`（HTTP 400，前端表现为报错横幅卡死）。**已落地的临时兜底**（commit `00bac9a` / `33c5b6f` / `fd1e19e`）：`lib/db/cloudbase.ts` 强制 `Connection: close` + 并发栅栏 3 + 重试 7 + `isTransientSqlError` 识别；`app/api/requirements/[id]/conversation/route.ts` 软降级；`components/requirements/conversation-panel.tsx` 横幅多事件清除 + 8s 自动消失。**暂不迁移**。根治路径二选一：① 迁到 PG 协议 `pg.Pool`（transaction 模式）；② 向控制台/工单调大 `pool_size`（成本低，优先先试）。**触发条件**：上生产后观察并发用户量。

## 三、MVP 后续 / 规划中未启动项（P1/P2，详见 `docs/phase1-backlog.md`）

### 3.1 已规划但尚未实现（按代码核实）
- [ ] **外部工具连接器（MCP 双向调用）**：`lib/ai/connectors/external/` 目录**为空**，仅有抽象骨架 `McpConnector` 在 `types.ts` 声明（`external: "MCP 对接设计工具"`）。规划里 P1/P2 的「AI 通过 MCP 调用真实第三方工具（Figma/真实接口等）」**未实现**——当前 MCP 仅「单向对外暴露产物」，AI 不会反向调工具。
  - **落地方案文档**：`docs/ops/external-connector-framework.md`（轻量自研框架，**不引入 cordis**；含 `external/{types,registry,index}.ts` + `adapters/figma.ts` 示例 + `mcp/server.ts` 改造 + 配置驱动热插拔 + 迁移 cordis 退路）。未来做到时直接参考该文档第七节检查清单。
- [x] **知识库检索（search_knowledge）**：**已由 M4 知识复利接入**（原标注「M2 占位」是订正前的误记，实际属 M4）。`app/api/mcp/knowledge/search/route.ts` 已改为真实语义检索（POST，`projectId`+`query`），MCP `search_knowledge` 工具与 `lib/services/knowledge.ts` 已落地。
- [x] **M1 数据迁移/裁剪方案（`docs/ops/m1-data-cutover.md`）**：已于 2026-08-11 实测确认——连上真实 CloudBase PostgreSQL（B 通道 `exec-pgsql`，`db:setup:cloudbase` 幂等建库脚本跑出 `21 张业务表全部就绪 ✓`，35 条语句已存在、pgvector 扩展已装）。表结构已落地、可写入，迁移实际已完成。

### 3.2 规划深度待增强（已实现基础版，需加深）
- [ ] **方案设计深度**：当前为单轮生成 + 原型；规划里的多方案对比、约束校验闭环未做。
- [ ] **原型 ↔ DevContext 闭环校验**：原型生成后未回写/校验 DevContext 一致性。
- [ ] **客户端接入示例**：`docs/mcp-guide.md` 提到的「把 AskBuddy 接进 CodeBuddy/Claude/Cursor」客户端配置示例可能未补。

## 四、SQL 型数据库（已达成，2026-08-11 实测确认）
- [x] **已改用 CloudBase SQL 型数据库（PostgreSQL）替代文档型**：用户指出文档型局限（无 JOIN / 跨集合事务弱 / 复杂关系查询不便）。
  - **现状（`DB_BACKEND=cloudbase`，`.env.local`）**：经 **CloudBase PostgreSQL 网关 B 通道 `exec-pgsql`（`Role=cloudbase_postgres`）** 访问，**这就是 SQL 型数据库**。`2026-08-11` 运行 `npm run db:setup:cloudbase` 实测：连真库成功、`pgvector` 扩展已装、**21 张业务表全部就绪**、可写入。文档型 NoSQL 仅作为 `lib/storage` 的回落通道。
  - **与旧规划的差异（重要）**：原规划评估的是「直连 MySQL（`mysql2` + `DATABASE_URL`）」并新增 `lib/db/sql.ts`；**实际落地的是「CloudBase PostgreSQL 网关通道」**，`DATABASE_URL` 仍为空、直连 PG/MySQL 那条未用。功能上"改用 SQL 型数据库"目标已达成，但实现路径与原始评估（MySQL 直连）不同——无需再补 `lib/db/sql.ts` 的 MySQL 分支。
  - **EMAXCONNSESSION 仍然有效**：因走的是网关 `exec-pgsql`（SQL 通道 session 模式），环境级 `pool_size=10` 共享硬上限限制**仍在**（见「二」临时兜底）。若未来并发上来要根治，可选：① 改直连 PG 协议 `pg.Pool`（transaction 模式，绕开网关 session 限制）；② 控制台/工单调大 `pool_size`。
  - **结论**：本条目标已达，从「待评估」移出；后续无强制动作，仅在并发告警时处理 EMAXCONNSESSION。

## 五、当前状态备注（非待办，仅备忘）
- ESLint 已配置（`.eslintrc.json`，extends `next/core-web-vitals`，非 strict）；`next lint` 无 Error，`next build` 门禁通过。
- 已知非阻断 Warning：`app/layout.tsx` 自定义字体触发 `@next/next/no-page-custom-font`（App Router 下为误报，不阻塞构建），暂不处理。
- **输出物侧边栏刷新已从 4 秒轮询改为事件驱动**（`requirement-shell.tsx` / `conversation-panel.tsx`）。调研分析、方案设计、需求文档等生成完成后已在 `requirement-shell.tsx` 的生成完成回调中调用 `globalMutate` 刷新 steps 与 outputs 缓存（含 SSE `step_update` 事件即时更新）。
- **测试现状**：M3 相关测试齐全（`devcontext-render.test.ts` / `devcontext-validate.test.ts` / `schemas/devcontext.test.ts` / `ai/context/builder.test.ts` / `db-pushdown-parity.test.ts`）。**尚缺** AI step（research/design/prd/prototype）、proposals、outputs 聚合、library 的单元测试，后续补测建议从这里入手。

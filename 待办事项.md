# 待办事项（AskBuddy）

记录目前尚未处理、未来某个节点需要处理的事项（非开发进度日志）。

## 一、接入真实 CloudBase（Phase 2）
- [x] 开 CloudBase 环境，配置 ApiKey（`CLOUDBASE_SECRET`，node-sdk `accessKey` 方式）。
- [x] `npm i @cloudbase/node-sdk` + `ws`。
- [x] 数据层接入 CloudBase **文档型（NoSQL）数据库**：`lib/db` 从内存改为 NoSQL 集合，`lib/cloudbase`、`lib/storage`（未配 COS 时回落 NoSQL）已接，`.env.local` 置 `USE_MOCK=false`。已验证「重启不丢数据」。
- [ ] Webify 部署（`next build` 已通过，待实际部署）。
- [ ] **（待定）评估是否改用 CloudBase SQL 型数据库**：当前为文档型，见下方「待评估」一节。

## 二、延后任务（已确认暂时不改，后续按需处理）
- [ ] **对象存储 COS**：`COS_BUCKET` / `COS_REGION` 仍空。当前原型 HTML / 导出文件临时存 NoSQL，未走 COS。需接时再实现 `lib/storage` 的 COS 分支。
- [ ] **真实多用户 Auth**：当前固定开发用户 `mock-user-001`（`lib/auth/session.ts`）。需补一套客户端登录 UI（@cloudbase/js-sdk 的 signIn，邮箱/微信登录），是独立功能，未做。
- [x] **AI 模型服务（主链路已完成）**：真实 CloudBase AI 已端到端打通，从需求对话到调研分析、方案设计、需求文档的完整流程均走真实生成（harness 架构见「三」）。保留 `AI_MOCK` 开关可强制走结构化占位，便于本地无凭证开发。

## 三、MVP 内即将实现（紧随当前功能）
- [x] **实现 `/dashboard/requirements` AI 对话创建需求页（创建时机 + 项目判定已就绪）**：`app/dashboard/requirements/page.tsx` 进入页面**不创建任何东西**，仅按 `?projectId` 判定模式——`project` 模式（带 projectId，在某项目下创建）与 `no-project` 模式（控制台橄榄页进入，无项目）。**首条对话才创建**：`project` 模式在该项目下建需求；`no-project` 模式先建新项目再建其下第一条需求（同名项目自动加随机后缀回落）。`ConversationPanel` 已重构为支持「需求尚未创建」的待定态（`requirementId: string | null` + `onFirstSend` 首条消息回调），用 `globalMutate` 规避 key 切换竞态；需求详情页的原用法（传真实 id）保持向后兼容。标题输入与「完成创建」仅在需求创建后展示，「完成创建」写回标题并生成卡片、跳转需求详情页；后端 `PATCH` 支持更新标题（`updateRequirementTitle`）。所有「新建需求」入口已统一指向该页，**去掉手动创建入口**。**对话功能已开发完成**：已建成轻量自研 harness 架构——AI 调用管理 `lib/ai/client.ts`（基于 @cloudbase/node-sdk，路由/流式/重试降级/mock 开关）+ 路由表 `lib/ai/models.ts`、Prompt 注册表 `lib/ai/prompts/`（对话访谈 + 卡片抽取，JSON 约束 + 变量插值）、上下文构建 `lib/ai/context/builder.ts`、连接器注册表 `lib/ai/connectors/`（内部 `save-card` 写回卡片；外部 MCP 仅留抽象骨架 `McpConnector`，P1/P2 实现）、Orchestrator `lib/ai/orchestrator.ts`（组装上下文→取 prompt→流式生成→Zod 解析→派发连接器）。对话接口 `app/api/requirements/[id]/conversation` 改为 SSE 流式，每轮返回 `{reply, card}`，前端双栏实时填充结构化需求卡片（`components/requirements/requirement-card-preview.tsx`），「完成创建」写回标题+卡片并推进阶段（注：早期实现曾置 `confirmed` 状态，该状态已随 4 步模型重构废弃，现由 `requirement_steps` 的确认闸门 `awaitingConfirm` 承担确认语义）。真实 CloudBase AI（hunyuan-lite）已端到端打通，结构化抽取（prompt 约束 JSON + Zod 校验）与渐进填充可用；`AI_MOCK` 可强制走结构化占位。后续任务类型（调研/分析/方案/原型/PRD）按计划 P1/P2 接入该 harness。

## 四、待评估（已决策）
- [x] **改用 CloudBase SQL 型数据库替代文档型（决策：做，但延后）**：用户指出文档型局限（无 JOIN / 跨集合事务弱 / 复杂关系查询不便）。已评估改动量（见会话记录）：复用现有 `db` 接口、后端换 MySQL（mysql2 + `DATABASE_URL`），新增 `lib/db/sql.ts` 做驼峰↔蛇形映射 + 谓词→WHERE 编译；当前 6 张在用表（`projects/requirements/conversations/prototypes/prototype_versions/api_tokens`）+ 存储回落用 `objects` 表；顺手修 `prototypes` 的 idKey bug。**前置条件：需在 CloudBase 控制台启用 MySQL 型数据库并提供 `DATABASE_URL`。**
  - **计划时机：MVP 功能全部做完后再改**（当前先用 NoSQL 跑通 MVP，避免打断功能开发）。
  - 执行前需用户：启用 CloudBase MySQL + 提供 `DATABASE_URL`（形如 `mysql://user:pass@host:port/db`）。

## 五、当前状态备注（非待办，仅备忘）
- ESLint 已配置（`.eslintrc.json`，extends `next/core-web-vitals`，非 strict）；`next lint` 无 Error，`next build` 门禁通过。
- 已知非阻断 Warning：`app/layout.tsx` 自定义字体触发 `@next/next/no-page-custom-font`（App Router 下为误报，不阻塞构建），暂不处理。
- **输出物侧边栏刷新已从 4 秒轮询改为事件驱动**（`requirement-shell.tsx` / `conversation-panel.tsx`，2026-07-18 优化，大幅降低 CloudBase 消耗）。当前仅"对话完成后"触发 `mutate('/api/requirements/${id}/outputs')` 刷新侧边栏。**复查提醒（2026-07-24 复核：已落实）**：调研分析、方案设计、需求文档等生成流程完成后，已在 `requirement-shell.tsx` 的生成完成回调中调用 `globalMutate` 刷新 steps 与 outputs 缓存（含 SSE `step_update` 事件的即时更新）。后续如再新增其他异步输出物生成来源，仍需保持同样的完成回调刷新约定。
